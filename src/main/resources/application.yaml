spring:
  application:
    name: Payment


  datasource:
    url: jdbc:postgresql://localhost:5432/payment_demo
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true